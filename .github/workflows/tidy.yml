name: add guardrailes to actions-journey

on: push

concurrency:
  group: ci-${{ github.ref }} 
  cancel-in-progres: true

jobs:
  guardrails:
    runs-on: ubunut-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - run: ls -l
