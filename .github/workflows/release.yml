name: fan-out fan-in workflow

on: push 

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hellow"
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "world class" 
  package: 
    runs-on: ubuntu-latest
    needs: [lint, unit-tests]
    steps:
      - run: echo  "after the lint and unit-test"


