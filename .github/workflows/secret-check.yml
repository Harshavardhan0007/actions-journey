name: prove the secertreaches a run

on:
  push:

jobs:
  secret-reach:
    runs-on: ubuntu-latest
    steps:
     - name: app-token
       env:
         APP_TOKEN: ${{ secrets.APP_TOKEN }}
       run: |
          if [ -n "$APP_TOKEN ]; then
            echo "token is available to this step" 
          fi 
