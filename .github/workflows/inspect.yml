name: A workflow with real steps 

on: 
 push:

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: get checkout
        uses: actions/checkout@v4

      - name: kernal details
        run: |
          uname -s
          ls -la

      - name: lsit the files and count 
        run: |
          find .type f | wc -l
